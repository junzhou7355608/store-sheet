{
  "name": "store-sheet",
  "version": "1.0.0",
  "description": "店铺数据统计",
  "type": "module",
  "scripts": {
    "gen:template": "tsx src/scripts/json-to-xlsx.ts",
    "gen:data": "tsx src/scripts/xlsx-to-json.ts",
    "copy:template": "for f in src/render-template/*-模板.xlsx; do [ -f \"$f\" ] && b=$(basename \"$f\" .xlsx) && cp \"$f\" \"src/render-data/${b%-模板}.xlsx\"; done",
    "gen": "pnpm gen:data && pnpm gen:template && pnpm copy:template && pnpm gen:data"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
